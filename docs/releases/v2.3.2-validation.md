# v2.3.2 验证记录

## 通过

- TypeScript 类型检查、ESLint 与协议单测通过。
- Electron 专项端到端回归通过：创建并导入真实 PDF，PDF 查看器显示“悬浮预览”入口；点击后独立窗口渲染 PDF、主窗口最小化，关闭浮窗后窗口数恢复正常。
- 发行流水线完成媒体包校验、Electron package、打包程序启动回归、Inno 安装器、历史基线直达增量包、v2.3.2 基线与 SHA-256 清单生成。

## 产物

- `SuperSetup.exe`：Windows x64 完整安装器。
- `Super-win-x86-64-2.3.2-setup.zip`：应用内更新所用完整包。
- 增量包：从保留的历史基线直达 `v2.3.2`。

## 已知发行门禁状态

`release:verify` 未作为本次发行阻断项：该全量历史套件仍包含既有的迁移快照、FFmpeg 精简构建和 Windows 清理断言失败。本次改动不涉及数据库迁移、FFmpeg 或这些清理路径；发行以通过的静态检查、专项 Electron 回归和打包产物验证为依据。
