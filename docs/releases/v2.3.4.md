# Super Lib v2.3.4

## 本版重点

- 左下角资源库卡片仅显示当前资源库名称，不再展示本地文件路径。
- 移除左下角资源库卡片旁的信息按钮，保留资源库切换与设置入口。
- 保持亚克力透明背景、资源库切换菜单及自动更新机制不变。

## 更新交付

本版沿用增量优先、完整包回退、断点续传和 SHA-256 校验规则。ECS 保留最近 5 个完整发行目录及最多 20 个增量来源版本；GitHub Release 与 ECS 使用同一组发行产物。
